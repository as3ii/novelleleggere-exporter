# Novelleleggere Exporter

Export an entire novelleleggere series as ebook/epub

To use run:
```bash
python -m venv
. ./bin/activate
pip install -r requirements.txt
python novelleleggere-exporter.py name of the series
```

